[Unit]
Description=prelude heartbeat daily 10:30 KST timer

[Timer]
# publish (10:10) 뒤 20분 후 — pipeline lock으로 겹침도 방지.
OnCalendar=*-*-* 10:30:00 Asia/Seoul
Persistent=true
RandomizedDelaySec=60

[Install]
WantedBy=timers.target
